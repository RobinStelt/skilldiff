/**
 * Visually distinguishes pre-fill data (Phase 6) from organic community
 * data (briefing point 7) — never indistinguishable from real runs.
 */
export function SeedDataBadge() {
  return (
    <span className="seed-data-badge" role="status">
      Seed data — small sample size
    </span>
  );
}
